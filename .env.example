# Browser-facing local service configuration.
# Values prefixed with PUBLIC_ are bundled into the frontend.

PUBLIC_DIRECTUS_URL=http://localhost:8055
PUBLIC_DIRECTUS_KEYCLOAK_PROVIDER=keycloak
PUBLIC_AGENTNEXUS_ISSUER=http://localhost:4321
